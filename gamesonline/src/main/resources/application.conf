my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
}
mongo {
  database = "gameonline"
  uri = "mongodb://ms@127.0.0.1:27017/"${mongo.database}
}

